// Shown on the title screen and in the builder so it's always possible to
// tell which build a device is running. Keep in sync with the CACHE name
// in sw.js — bump both on every deploy.
export const VERSION = 'v7';
